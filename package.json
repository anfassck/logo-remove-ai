{
  "name": "cleanframe-ai",
  "version": "1.0.0",
  "description": "CleanFrame AI - AI-Assisted Video Restoration & Logo Removal",
  "main": "server.js",
  "type": "module",
  "scripts": {
    "start": "node --max-old-space-size=256 server.js",
    "build": "npm install && cd backend && npm install",
    "postinstall": "cd backend && npm install",
    "dev:backend": "cd backend && npm start",
    "dev:frontend": "cd frontend && npm run dev",
    "install:all": "npm install && cd backend && npm install && cd ../frontend && npm install"
  },
  "dependencies": {
    "cors": "^2.8.5",
    "dotenv": "^16.4.7",
    "express": "^4.21.2",
    "ffmpeg-static": "^5.2.0",
    "ffprobe-static": "^3.1.0",
    "fluent-ffmpeg": "^2.1.3",
    "multer": "^1.4.5-lts.1",
    "uuid": "^11.1.0"
  }
}
